services:
  postgres:
    image: postgres:16-alpine
    ports:
      - "5432:5432"
    environment:
      POSTGRES_DB: manifestacije
      POSTGRES_USER: manifestacije
      POSTGRES_PASSWORD: manifestacije
    volumes:
      - postgres-data:/var/lib/postgresql/data

volumes:
  postgres-data:
